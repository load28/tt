# TASK-170: 로컬 저장소와 Git 원격을 tt로 전환

- **상태**: 완료
- **시작일**: 2026-08-23
- **완료일**: 2026-08-23
- **커밋**: 65ab92b

## 목적

GitHub 저장소 개명에 맞춰 로컬 체크아웃 경로와 `origin` 원격을 `tt`로 전환한다.

## 범위

- 포함: 로컬 저장소 디렉터리 `rl` → `tt`, 로컬 개발 상태 `.rl-dev` → `.tt-dev`, 로컬 npm 개발 설정의 저장소 루트, Git `origin` URL 변경과 연결 검증
- 제외: 프로젝트 소스의 추가 개명, 버전 변경

## 의사결정

### 결정 1: 기존 체크아웃을 제자리에서 이동한다

- **상황**: 저장소 내용과 Git 메타데이터를 유지하면서 로컬 경로만 새 이름에 맞춰야 한다.
- **검토한 대안**: 새로 clone하면 깨끗하지만 로컬 생성물과 설정을 다시 구성해야 한다. 현재 디렉터리를 이동하면 같은 체크아웃과 로컬 상태를 그대로 유지한다.
- **선택과 근거**: 대상 경로가 비어 있음을 확인한 뒤 현재 체크아웃 전체를 `tt`로 이동한다.

### 결정 2: origin은 개명된 저장소 URL로 직접 교체한다

- **상황**: 기존 `origin`이 `load28/rl.git`을 가리킨다.
- **검토한 대안**: GitHub 리디렉션에 의존할 수 있지만 로컬 설정에 이전 이름이 남는다. URL을 교체하면 fetch와 push 대상이 명시적으로 새 저장소가 된다.
- **선택과 근거**: `origin`을 `git@github-load28:load28/tt.git`으로 바꾸고 원격 조회로 검증한다.

## 작업 내역

- 2026-08-23: 현재 경로, 대상 경로 충돌, 작업 트리, 원격 URL과 로컬 개발 설정을 확인했다.
- 2026-08-23: `.rl-dev`를 `.tt-dev`로 이동하고 `npm/tt-lang/tt-dev.local.json`의 로컬 저장소 루트를 새 경로로 바꿨다.
- 2026-08-23: 체크아웃을 `/Users/seominyong/Downloads/source/tt`로 이동했다.
- 2026-08-23: `origin`을 `git@github-load28:load28/tt.git`로 바꾸고 fetch와 upstream 동기화를 확인했다.

## 이슈 및 해결

### 이슈 1: 체크아웃 이동 후 문서 편집 도구가 새 경로를 쓰지 못했다

- **증상**: `/Users/seominyong/Downloads/source/tt`에서 명령 실행은 가능했지만 `apply_patch`가 새 경로를 찾지 못했다.
- **원인**: 현재 세션의 쓰기 가능 작업 루트가 시작 시점의 `.../source/rl`로 고정돼 있었다.
- **해결**: 체크아웃을 잠시 이전 경로로 되돌려 태스크 문서를 갱신한 뒤 최종 경로로 다시 이동했다.

## 검증

- [x] 로컬 경로가 `/Users/seominyong/Downloads/source/tt`인지 확인
- [x] `origin` fetch/push URL이 `load28/tt.git`인지 확인
- [x] `git fetch origin`과 upstream 동기화 확인

## 결과

로컬 체크아웃과 개발 상태 디렉터리를 `tt` 경로로 이동했고 Git fetch/push 원격을 개명된 `load28/tt` 저장소로 전환했다.
