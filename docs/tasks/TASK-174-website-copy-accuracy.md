# TASK-174: 웹사이트 문구 정확성 전면 점검

- **상태**: 완료
- **시작일**: 2026-08-23
- **완료일**: 2026-08-23
- **커밋**: —

## 목적

웹사이트의 언어·도구 설명을 현재 `main`의 구현 계약과 일치시킨다. 각 주제의 유의사항 섹션을 제거하고 기능 설명에 집중한다.

## 범위

- 포함: 영문·한글 웹사이트 문구와 예제의 구현·테스트 대조, 부정확한 설명 수정, 유의사항 섹션과 관련 UI 제거, 웹사이트 및 저장소 검증 게이트 실행
- 제외: 컴파일러 동작 변경, 새 언어 기능 추가, 웹사이트 디자인 개편, 배포

## 의사결정

### 결정 1: 현재 내장 AI 가이드와 실행 테스트를 문구 검증 기준으로 사용한다

- **상황**: 웹사이트 문구 일부가 이전 코드 생성 방식을 설명하고 있어 현재 구현을 대표하는 기준이 필요했다.
- **검토한 대안**: 기존 웹사이트 문구만 부분 수정하면 변경 범위가 작지만 다른 오래된 설명을 남길 수 있다. 구현 파일만 직접 읽으면 사용자 계약보다 내부 세부에 치우칠 수 있다.
- **선택과 근거**: `docs/ai/tt.md`의 사용자 계약을 기준으로 삼고, 중요한 동작은 `tests/compile.rs`·`tests/integration.rs`와 실제 컴파일 결과로 교차 검증한다.

### 결정 2: 유의사항 데이터와 렌더링을 함께 제거한다

- **상황**: 각 주제에서 기능 설명만 남기려면 화면뿐 아니라 콘텐츠 모델도 단순화해야 한다.
- **검토한 대안**: UI에서만 숨기면 사용되지 않는 문구가 계속 단일 콘텐츠 원본에 남는다. 문구를 기능 목록에 합치면 요청한 기능 중심 설명이 다시 제약 목록으로 흐려진다.
- **선택과 근거**: 모든 `limits` 데이터를 제거하고 `ReferencePage`가 기능 목록 하나만 렌더링하도록 바꾼다.

## 작업 내역

- 2026-08-23: `website/src/content.json`, `website/src/ui/ReferencePage.tsx`, 내장 AI 가이드와 관련 컴파일 테스트를 조사했다.
- 2026-08-23: `match`와 `result` 블록의 `await` 설명을 statement lowering 우선·표현식 전용 awaited boundary 폴백 계약으로 수정했다.
- 2026-08-23: TypeScript·TSX 통과, 상대 import 재작성, 설치기 설정 보존, `.ttx` 원본 보존, typed CLI 위치 설명을 현재 구현 범위에 맞게 좁혔다.
- 2026-08-23: 웹사이트의 영문·한글 `사용 시 주의사항`/`Things to know` 렌더링을 제거하고 단일 `기능`/`Features` 목록으로 정리했다.
- 2026-08-23: 웹사이트 `typecheck`와 33개 경로의 프로덕션 프리렌더 빌드를 통과시켰다.
- 2026-08-23: Rust 포맷·clippy·전체 테스트 게이트를 독립 빌드 디렉터리에서 통과시켰다.

## 이슈 및 해결

### 이슈 1: 저장소 개명 전 경로가 Cargo 테스트 산출물에 남아 있었다

- **증상**: 기본 `target`에서 `cargo test`를 실행하면 CLI 테스트 32개가 `failed to run ttc: No such file or directory`로 실패했다.
- **원인**: 캐시된 CLI 테스트 바이너리의 `CARGO_BIN_EXE_ttc`가 현재 `/source/tt`가 아닌 개명 전 `/source/rl/target/debug/ttc`를 가리켰다. `strings target/debug/deps/cli-*`로 내장 경로를 확인했다.
- **해결**: 기존 산출물을 삭제하지 않고 `CARGO_TARGET_DIR=/tmp/tt-task-172-target`에서 clippy와 전체 테스트를 새로 빌드해 검증했다.

## 검증

- [x] `cargo fmt --check`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo test`
- [x] 웹사이트 타입 검사와 프로덕션 빌드

## 결과

웹사이트의 모든 사용자 노출 주제를 현재 내장 가이드와 테스트에 대조했다. 오래된 async IIFE 일반화와 과도한 passthrough·설치기 표현을 고쳤으며, 각 페이지는 예제·요약·기능 목록만 표시한다. 컴파일러 동작은 바뀌지 않아 `docs/ai/tt.md` 갱신은 필요하지 않다.
