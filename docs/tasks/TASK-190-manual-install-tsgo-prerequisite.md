# TASK-190: 수동 설치의 typescript-go 선행 조건 명시

- **상태**: 완료
- **시작일**: 2026-08-23
- **완료일**: 2026-08-23
- **커밋**: 이 변경을 포함하는 커밋

## 목적

컴파일러 수동 설치에서 `typescript@7` 설치가 ttc의 sync API 도구 체인까지
제공하는 것으로 오해되지 않게 한다. 문서 상단의 typescript-go 소스 빌드와
`TTC_TSGO_ROOT` 설정이 먼저 완료되어야 함을 설치 단계 바로 옆에 명시한다.

## 범위

- 포함: 공식 홈페이지, 영문·한글 README와 설치 가이드, npm 컴파일러 README의
  수동 설치 선행 조건 문구.
- 제외: 의존성·컴파일러·설치기 동작 변경.

## 의사결정

### 결정 1: 상단 설명 참조와 필수 조건을 수동 설치 위치에서 반복

- **상황**: 상단에 typescript-go 빌드가 있지만 수동 설치 제목 아래에는
  `typescript@7` 설치 명령만 있어 독립 절차처럼 읽힌다.
- **검토한 대안**: 상단 설명만 유지 / 수동 설치 코드 전체를 다시 복제 / 수동
  설치 위치에서 상단 절차 완료와 환경변수 유지가 필수임을 짧게 명시.
- **선택과 근거**: 세 번째 방식을 선택한다. 빌드 명령의 중복·불일치를 피하면서
  `typescript@7` npm 의존성과 ttc가 실제 사용하는 소스 빌드 toolchain의 역할을
  분리해 설명할 수 있다.

## 작업 내역

- 2026-08-23: 공식 홈페이지와 영문·한글 설치 가이드의 수동 컴파일러 문구를
  확인했다.
- 2026-08-23: 수동 컴파일러 절차 바로 위에 typescript-go 빌드 완료와
  `TTC_TSGO_ROOT` 유지가 필수라는 문구를 추가하고 `typescript@7`의 역할을
  프로젝트 의존성으로 한정해 설명했다.
- 2026-08-23: 직접 ttc를 사용하는 수동 번들러 절차에도 같은 선행 조건을
  명시하고, README와 npm 컴파일러 패키지 문구를 일치시켰다.
- 2026-08-23: 웹 하이라이트 생성, 타입 검사, 33개 경로 prerender 빌드와 Rust
  검증 게이트 전체를 실행했다.

## 이슈 및 해결

없음.

## 검증

- [x] `cargo fmt --check`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo test`
- [x] `website`: `bun run typecheck`
- [x] `website`: `bun run build` (33개 경로 prerender)

## 결과

공식 홈페이지와 GitHub의 모든 수동 컴파일러·번들러 절차가 상단의
typescript-go 소스 빌드와 `TTC_TSGO_ROOT` 설정을 필수 선행 조건으로 가리킨다.
`typescript@7`은 프로젝트 의존성이며 해당 도구 체인을 대신하지 않는다고
명시했다.
