# TASK-221: `unwrap`/`expect` 감사 — 안전망에서 사고 자체로

- **상태**: 대기
- **시작일**: —
- **완료일**: —
- **커밋**: —

## 목적

TASK-214는 컴파일러가 패닉할 때 **어떻게 보고하고 살아남는지**를 정했다. 사고를
없애지는 않았다. `src/`의 현황(grep 기준):

- `unwrap()` 78
- `expect(` 100
- `panic!` / `unreachable!` 77

전부가 문제는 아니다. `InternalCompilerError::raise()`처럼 깨진 계약을 알리는
패닉은 의도된 것이고, 검증기가 이미 보장한 불변식에 기대는 `unwrap`도 있다.
문제는 **어느 것이 어느 쪽인지 구분이 문서화되어 있지 않다**는 점이다. 사용자
입력으로 도달 가능한 `unwrap`은 컴파일러 버그이고, 도달 불가능한 것은 그 이유가
적혀 있어야 한다.

## 범위

- 포함:
  - 세 부류로 분류: (a) 사용자 입력으로 도달 가능 — 진단이나 총함수로 고친다,
    (b) 상위 계층이 보장하므로 도달 불가 — `expect`의 메시지에 **왜** 도달
    불가한지 쓴다, (c) 이미 의도된 ICE — `ice.rs`의 명명된 invariant로 표현할 수
    있는지 검토한다.
  - 우선순위는 사용자 입력에 가까운 계층부터: `lexer` → `parser` → `scanner` →
    `hir` → `sema`. `codegen/core.rs`(49건)와 `hir/lower.rs`(32건)가 가장 많다.
- 제외:
  - 일괄 치환. `unwrap`을 기계적으로 `?`로 바꾸면 오류 경로가 늘어나기만 하고
    무엇이 보장되는지는 여전히 적히지 않는다. 이 태스크의 산출물은 "패닉이 줄었다"가
    아니라 "남은 패닉마다 그것이 왜 도달 불가한지가 코드에 적혀 있다"이다.

## 의사결정

## 작업 내역

## 이슈 및 해결

## 검증

- [ ] `cargo fmt --check`
- [ ] `cargo clippy --all-targets -- -D warnings`
- [ ] `cargo test`

## 결과
