# TASK-224: 커버리지 측정과 게이트

- **상태**: 완료
- **시작일**: 2026-08-25
- **완료일**: 2026-08-25
- **커밋**: (아래 "작업 내역")

## 목적

40k 줄이 넘는 컴파일러에서 **어느 경로가 테스트되지 않는지 아무도 모른다**.
커버리지 측정이 없기 때문이다. 테스트가 많다는 것과 중요한 경로가 덮여 있다는
것은 다른 이야기이고, 지금은 후자를 확인할 방법이 없다.

특히 큰 파일들이 궁금하다: `codegen/core.rs` 3079줄, `program_syntax.rs` 3161줄,
`evaluation_ir.rs` 2357줄, `analysis/mod.rs` 2263줄.

## 범위

- 포함:
  - `cargo-llvm-cov`를 CI에 추가하고 보고서를 산출물로 남긴다
  - 현재 수치를 측정해 기준선으로 기록한다
  - **하한선 게이트**: 기준선보다 떨어지면 실패
  - 덮이지 않은 영역 중 사용자 입력으로 도달 가능한 것을 목록화한다
- 제외: 커버리지를 올리기 위한 테스트 작성. 측정이 먼저다.

## 의사결정

### 1. 커버리지 잡은 TypeScript 툴체인을 **요구**한다

측정해 보니 같은 저장소가 두 개의 숫자를 갖는다.

| 구성 | 줄 커버리지 |
|---|---|
| typescript-go 있음 | **86.84%** |
| 없음 | 80.72% |

6포인트 차이는 오차가 아니라 **다른 프로그램**을 잰 것이다. 툴체인이 없으면
`typescript/service.rs`가 3.5%, `engine/language.rs`가 30.6%로 떨어진다 — 에디터와
백엔드 계층이 아예 돌지 않는다. 실행마다 이 둘 사이를 오가는 숫자로는 "떨어지면
실패" 게이트를 만들 수 없다.

그래서 커버리지 잡은 `native` 잡과 **같은 고정 revision**의 typescript-go를
빌드하고 `TTC_REQUIRE_TSGO=1`을 세운다. 툴체인이 없으면 숫자가 조용히 낮아지는
대신 잡이 실패한다 — 이 게이트가 거짓말할 수 있는 유일한 경로를 막는 것이다.

### 2. 하한선은 측정값, 목표치가 아니다

`--fail-under-lines 86.9`. 측정값을 그대로 쓰지 않는 이유가 있고, 그 값을
직접 겪었다.

같은 커밋을 네 번 재면 87.00 / 87.01 / 87.02 / 87.02가 나온다 — 미도달 줄이
3374~3379 사이에서 흔들린다. 처음에 87.02를 재고 하한선을 `87.0`으로 잡았더니
그 다음 실행(87.00, 그리고 내부적으로는 87.0을 살짝 밑도는 값)에서 게이트가
물었다. **변경이 없는데 빨간 게이트**는 게이트가 아니라 소음이다.

그래서 하한선은 관측된 최솟값 **아래**에 둔다. 폭이 0.02포인트이므로 0.1포인트
아래인 86.9다. "떨어지면 실패"의 의미는 그대로이고, 잡아내지 못하는 것은
0.1포인트 미만의 하락뿐이다 — 그 정도는 애초에 이 도구가 구별하지 못한다.

"떨어뜨리지 않는다"는 따를 수 있는 규칙이고, 임의의 목표 숫자는 숫자를 움직이기
위한 테스트를 부른다. 하한선을 올리는 것은 **커버리지를 벌어들인 변경의 몫**이다.

그 규칙이 이 브랜치 안에서 한 번 적용됐다. 기준선을 두 번 쟀는데 — 이 태스크
시점에 86.84%, TASK-221·TASK-222·TASK-223·TASK-229가 들어간 뒤 **87.02%** —
하한선은 후자에 맞춰 올렸다. 그 변경들이 같은 브랜치에 있기 때문이다.

### 3. 예상이 빗나간 곳 — 큰 파일이 문제가 아니었다

목적에서 걱정한 네 파일은 실제로 잘 덮여 있다.

| 파일 | 줄 커버리지 |
|---|---|
| `codegen/core.rs` | 90.67% |
| `program_syntax.rs` | 87.07% |
| `evaluation_ir.rs` | 93.04% |
| `analysis/mod.rs` | 95.34% |

약한 곳은 **크기가 아니라 위치**로 갈린다: Rust 테스트가 아니라 다른 언어의
테스트가 검증하는 계층이 낮다. `server.rs`(39.6%)와 `engine/language.rs`(42.8%)는
VS Code 확장의 JS 스위트가 실제로 검증하지만, 그 실행은 `cargo llvm-cov`에
보이지 않는다. 즉 이 두 숫자는 "테스트가 없다"가 아니라 "이 자에 안 잡힌다"에
가깝다 — 그래도 Rust 쪽에서 프로토콜을 직접 거는 테스트가 얇다는 사실은
그대로다.

## 작업 내역

1. `.github/workflows/ci.yml`에 `coverage` 잡 추가 — `llvm-tools-preview` +
   `cargo-llvm-cov`, 고정 revision의 typescript-go 빌드,
   `--fail-under-lines 86.9`, `lcov.info` artifact.
   `scripts/ci`에 같은 게이트를 `coverage` 단계로 두었다 — TASK-227이 CI를
   로컬 실행으로 옮겼으므로, 워크플로에만 있는 게이트는 아무도 돌리지 않는다.
   기본 실행에는 넣지 않았다(몇 분이 걸리고, 이 변경이 옳은가에 혼자 답하지
   못한다). 이름을 대면 돈다: `./scripts/ci coverage`.
2. `tests/engine_cache.rs`의 `an_error_node_keeps_its_file_and_other_files_checkable`
   수정 (아래 "이슈 및 해결").
3. 기준선과 취약 목록을 이 문서에 기록.

## 이슈 및 해결

- **증상**: 툴체인 없는 구성에서 커버리지를 재려 하자
  `an_error_node_keeps_its_file_and_other_files_checkable`가 실패해 측정 자체가
  멈췄다. (같은 실패는 이 브랜치를 시작할 때 `cargo test`에서도 보였다.)
- **원인**: 이 케이스는 "블록된 파일이 자기 진단을 유지하고 이웃 파일은 계속
  검사된다"를 확인하는데, 이웃 파일의 진단으로 **exhaustiveness**를 골랐다.
  엔진 경로는 소진성을 체커에게 위임하므로(`defer_to_checker`), 백엔드가 없으면
  그 파일에는 진단이 하나도 없다. 테스트의 주제와 무관한 이유로 빨간 것이다.
- **해결**: 이웃 파일의 진단을 tt 계층이 스스로 답하는 규칙(중복 arm)으로
  바꿨다. 주제는 그대로이고, 이제 툴체인 유무와 무관하게 같은 것을 확인한다.
  TASK-217 결정 4와 같은 종류의 문제 — 전제 조건이 선언되지 않은 테스트다.
  같은 실패를 다른 브랜치가 **TASK-228**로 등록했다(다른 원인 가설과 함께).
  병합하면서 측정으로 정리했고, 자세한 내용은 그 문서에 있다.

## 검증

- [x] `cargo fmt --check`
- [x] `cargo clippy --all-targets -- -D warnings`
- [x] `cargo test` — 툴체인 있음/없음 두 구성 모두 초록
- [x] `./scripts/ci coverage` (= `--fail-under-lines 86.9`) → 통과
- [x] 같은 커밋 4회 측정으로 흔들림 폭 확인 (87.00~87.02)
- [x] 같은 명령에 `--fail-under-lines 90` → 실패 (게이트가 실제로 문다)

## 결과

### 기준선 — 2026-08-25, typescript-go `c6b013f5`

| 척도 | 값 |
|---|---|
| 줄 | **87.02%** (25990 중 3374 미도달) |
| 함수 | 88.36% (2336 중 272 미실행) |
| 리전 | 85.90% |

### 덮이지 않은 영역 중 사용자 입력으로 도달 가능한 것

TASK-221(패닉 감사)이 이어받을 목록이기도 하다 — 도달 가능한데 덮이지 않은
경로가 곧 "도달 불가"라고 적을 수 없는 `unwrap`이 사는 곳이다.

| 파일 | 줄 | 미도달 줄 | 무엇이 안 덮이나 |
|---|---|---|---|
| `server.rs` | 39.6% | 330 | JSON-lines 프로토콜의 요청 핸들러 대부분. 에디터가 매 키 입력마다 도달한다. 확장의 JS 스위트가 검증하지만 이 자에는 안 잡힌다 |
| `engine/language.rs` | 42.8% | 672 | LSP 기반 기능(hover, rename, references, signature help)의 절반. 위와 같은 사정 |
| `main.rs` | 71.8% | 370 | CLI 인자 조합과 오류 경로. 전부 사용자 입력이다 |
| `scanner.rs` | 74.8% | 100 | 스캐너의 드문 분기. **사용자 입력에 가장 가까운 계층** |
| `parser/enums.rs` | 73.5% | 57 | enum 선언의 실패 갈래 |
| `parser/literals.rs` | 68.4% | 81 | 리터럴 패턴의 형태들 |
| `engine/declarations.rs` | 71.4% | 56 | 선언 테이블의 드문 갈래 |
| `typescript/service.rs` | 70.4% | 85 | LSP 전송의 오류 경로 |
| `probe.rs` | 79.5% | 54 | 프로브 수집의 드문 갈래 |
| `engine/snapshot.rs` | 45.7% | 25 | 접근자 12개 중 6개가 한 번도 불리지 않는다 |

이 태스크는 여기서 멈춘다 — 숫자를 올리기 위한 테스트는 쓰지 않는다. 위
목록이 다음 작업들의 입력이다.

### 변경 파일

- `.github/workflows/ci.yml`, `scripts/ci`
- `CONTRIBUTING.md`
- `tests/engine_cache.rs`
- `docs/tasks/INDEX.md`
