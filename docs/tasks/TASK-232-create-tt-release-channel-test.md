# TASK-232: Dev 스탬프 이후 create-tt 채널 테스트 정합성

- **상태**: 완료
- **시작일**: 2026-08-26
- **완료일**: 2026-08-26
- **커밋**: —

## 목적

Dev 릴리스 버전으로 스탬프된 `create-tt`의 통합 테스트가 실제 릴리스 채널 계약을 검증하도록 고친다.

## 범위

- 포함: 설치기가 생성하는 tt 패키지 의존성 채널의 테스트 기대값
- 제외: 설치기 채널 선택 로직과 릴리스 버전 계산 변경

## 의사결정

### 결정 1: 현재 패키지 버전에서 기대 채널을 계산한다

- **상황**: 기본 개발 매니페스트에서는 `latest`, Dev 릴리스 매니페스트에서는 `dev`가 정상인데 테스트가 `latest`만 허용했다.
- **검토한 대안**: 릴리스에서 테스트 제외 / 릴리스에서도 `latest` 강제 / 패키지 버전에 맞춰 기대값 계산.
- **선택과 근거**: 같은 테스트가 기본 매니페스트와 릴리스 스탬프 매니페스트의 채널 계약을 모두 검증하도록 기대값을 계산한다.

## 작업 내역

- 2026-08-26: Prepare Dev Release 실행 32906875694의 실패 로그에서 두 채널 기대값 불일치를 확인했다.
- 2026-08-26: 통합 테스트가 현재 `create-tt` 매니페스트 버전에서 기대 의존성 채널을 계산하도록 바꿨다.

## 이슈 및 해결

### 이슈 1: Dev 릴리스 스탬프에서 설치기 테스트 두 건 실패

- **증상**: 기대값 `latest`와 실제값 `dev`가 달라 테스트가 실패했다.
- **원인**: 테스트가 저장소 기본 버전 `0.0.0-dev`의 채널만 고정해서 기대했다.
- **해결**: `dependencyChannel`로 현재 매니페스트 버전의 기대 채널을 계산해 생성·초기화 결과와 비교한다.

## 검증

- [x] `node --test packages/create-tt/test/*.test.mjs`
- [x] `./scripts/ci`

## 결과

기본 개발 매니페스트와 Dev 릴리스 매니페스트가 같은 테스트로 각자의 의존성 채널 계약을 검증한다.
