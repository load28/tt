# TASK-252: Stable 0.3 사용자 설치 가이드

- **상태**: 완료
- **시작일**: 2026-08-27
- **완료일**: 2026-08-27
- **커밋**: —

## 목적

사용자용 설치 가이드가 Nightly `next` 대신 공식 Stable `0.3.0`을 기본으로 안내하게
한다. Nightly는 릴리스 운영 문서에서 선택 채널로 유지한다.

## 범위

- 포함: README, 시작 가이드, npm 패키지 README, 번들러 안내, 웹사이트, AI 사용자 문맥
- 제외: Nightly 게시 방식과 npm dist-tag 운영 계약

## 의사결정

### 결정 1: 기본 설치 명령은 공식 Stable 버전을 명시한다

- **상황**: 현재 사용자 문서가 `@next`를 기본 설치 경로로 안내한다.
- **검토한 대안**: `@next` 유지 / `@latest` 사용 / 현재 Stable 버전 명시.
- **선택과 근거**: 아직 기능이 부족해도 공식 배포본이라는 사용자 판단 기준을 분명히
  하도록 컴파일러와 초기화 도구는 `0.3.0`을 명시한다.

## 작업 내역

- 2026-08-27: 사용자용 문서와 웹사이트의 `@next` 설치 명령을 전수 확인했다.
- 2026-08-27: 영문·한글 README, 시작 가이드, npm 패키지 README, 번들러 안내,
  웹사이트, AI 사용자 문맥의 기본 설치 명령을 Stable 버전으로 바꿨다.
- 2026-08-27: 웹사이트 구문 강조 캐시를 재생성하고 production build와 전체 로컬
  CI를 통과했다.

## 이슈 및 해결

없음.

## 검증

- [x] 사용자 문서의 기본 설치 명령 검색
- [x] 웹사이트 콘텐츠 JSON 파싱
- [x] `bun run build` (`website/`)
- [x] `./scripts/ci`
- [x] `git diff --check`

## 결과

사용자용 기본 설치 경로는 `@load28/create-tt@0.3.0`과
`@load28/tt-lang@0.3.0`을 안내한다. 번들러 플러그인은 현재 Stable인
`@load28/unplugin-tt@0.1.0`을 명시한다. Nightly `next` 안내는 릴리스 운영 문서와
기여자 검증 절차에만 남겼다.
