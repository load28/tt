import type { TOption } from "@tt/std";
declare function find(id: string): TOption<string>;

export function greet(id: string): string {
  const Some(value: name) = find(id) else {
    console.log("missing");
  };
  return name;
}
