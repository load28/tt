declare const input: unknown;
declare const flag: boolean;
declare function consume(item: { kind: "item"; run: (x: number) => number }): void;
consume(match (flag) {
  true if typeof input === "string" => ({ kind: "item", run: x => x + input.length }),
  _ => ({ kind: "item", run: x => x }),
});
